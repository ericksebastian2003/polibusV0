buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Asegúrate de tener AGP 8.2.1 o superior
        classpath 'com.android.tools.build:gradle:8.2.1'  // Asegúrate de usar una versión compatible
    }
}
